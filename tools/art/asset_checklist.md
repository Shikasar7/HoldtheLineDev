# 原型版 AI 美术资产清单与验收标准(gpt-image-2 全量生成)

风格圣经:`style_bible.json` **v1**(已锁定)。全部 245 条提示词已在 `out/prompts.json` 就绪,按下表 id 取用。

## 通用规则(适用所有条目)

- **提示词**:一律用 `out/prompts.json` 里的成品,不手写。要改风格 → 改 `style_bible.json` → 重跑 `generate_prompts.py`。
- **锚点图**:凡插画类(卡面/领袖/Key Art/结算图),生成时把 `anchors/` 里 3 张 v1 锚点图作为参考图一并给 gpt-image-2;UI 类(框/图标/纹理)不挂锚点,靠提示词里的色板词。
- **负面提示词**:gpt-image-2 不支持独立负面输入,`prompts.json` 的 `negative` 字段作为"验收否决表"使用——出现其中任何元素(文字、水印、卡框、3D 渲染感…)即打回。
- **候选数**:卡面/领袖每张 2~4 候选四选一;UI/棋盘/Key Art 每项 2~3 候选。
- **存放与命名**:`tools/art/generated/v1/<id>.png`,与 prompts.json 的 id 严格一致(已有 3 张锚点卡照此命名)。原始生成图全部保留,裁切/去背产物后续由 postprocess 脚本输出到 `game/assets/`,不手工裁。
- **通用验收**:①并排放进已验收图堆里不跳戏;②冷灰暖褐主调 + 辉尘青金点缀;③画内无任何文字/数字/边框。

## A. 卡面插画 — 49 张(核心工作量)

| 项 | 规格 |
|---|---|
| id | 各卡牌 id(iv_* 16 张、wp_* 16 张、neutral 17 张) |
| 生成尺寸 | **单位卡(36 张):1024×1536 竖版**——立牌取全身,竖版让人物撑满画面,分辨率利用率最高;**指令卡(13 张):1536×1024 横版**——无立牌,动作小景与卡框插画窗比例匹配 |
| 后处理去向 | 卡面从画面上/中部裁 4:3 入框;**单位卡额外去背裁全身立牌**,指令卡不出立牌 |

**单位卡验收**(36 张):单主体全身、含脚、3/4 视角、地面阴影;背景虚化干净(想象去背后轮廓是否完整不粘连);顶左侧轮廓光。**主体不完整/背景杂物粘身 = 打回**,这直接决定立牌质量。另外头顶和脚下各留一点呼吸空间(不顶天立地),卡面 4:3 裁切才有余地。

**指令卡验收**(13 张):单一视觉焦点的动作小景;缩到手牌大小(约 120px 宽)仍能一眼读懂在干什么;边缘暗化虚化。

**阵营辨识**:铁誓=钢灰蓝+誓火金;野群=兽皮褐+野性绿/骨白;中立=青铜/赭黄、**无阵营纹章**。盖住卡名能认出阵营为合格。

## B. 领袖头像 — 2 张

id:`leader_iv_valen`、`leader_wp_saen`。1024×1024(维持正方形——最终是圆形裁切,正方形是圆裁的自然来源),后处理 512 圆形裁切。
**验收**:半身像;脸和纹章在**圆形裁切内**完整可读(生成后拿圆框套一下);气场压得住选人界面。

## C. 对战场景 — 2 项

| id | 尺寸 | 验收 |
|---|---|---|
| `board_main` 棋盘底图 | 1536×1024 横 | **低对比、重去饱和**是硬标准——叠上立牌后立牌必须跳出来;画内无格线(程序画)、无图标文字;光照均匀无强投影 |
| `standee_base` 立牌底座 | 1024×1024 | 椭圆木底座单体;去背后与任意立牌合成不违和;只选 1 张定稿,所有立牌共用 |

## D. 主菜单 — 2 项

| id | 尺寸 | 验收 |
|---|---|---|
| `key_art_main` 主视觉 | 1536×1024 横 | 两领袖对峙构图与 B 的两张头像**同一人设**(装备/配色一致);**画面上中部留白简洁**,预留 Logo 位;此图是门面,值得多 roll 几轮 |
| Logo/标题字 | — | **不用 AI 生成**(文字生成不可靠),字体排版 + Godot 描边/渐变实现 |

## E. UI 套件 — 12 项(AI 出元素,排版交给 Godot Theme)

| id | 用途 | 尺寸 | 关键验收点 |
|---|---|---|---|
| `card_back` | 卡背/对手手牌 | 1024×1536 竖 | 严格左右对称;缩小到手牌尺寸不糊成一团 |
| `frame_iron_vow` / `frame_wildpack` / `frame_neutral` | 三阵营卡框 | 1024×1536 竖 | 上 2/3 有**干净的空矩形插画窗**,下 1/3 素面文本区;边框适合九宫格拉伸(四角装饰、四边纹样均匀);三张框窗口位置一致 |
| `gem_cost` / `gem_atk` / `gem_hp` | 费用/攻/血宝石 | 1024×1024 | 去背后缩到 **64px 仍清晰可辨**;三颗风格成套(同挂前两颗已过图作参考再生成第三颗);数字由程序叠加,图内无数字 |
| `emblem_iron_vow` / `emblem_wildpack` | 阵营纹章 | 1024×1024 | 剪影够强,单色化后仍可辨;用于选卡组界面/回合指示 |
| `texture_parchment` | 面板底纹 | 1024×1024 | 低对比、无明显重复痕迹;叠黑色半透明文字可读 |
| `button_plate` | 按钮底板 | 1536×1024 横 | 适合九宫格;素面无字(文字/hover/pressed 状态由 Godot 调色) |

**UI 类通用**:平视、对称、纯色深底方便去背;宁可朴素不可花哨——UI 是 AI 弱项,一旦某项 roll 3 轮仍不合格,**降级为程序绘制**(Godot StyleBoxFlat),不死磕。

## F. 结算插图 — 2 项(可选,P4 结算循环要用再生成)

`result_victory`(残旗迎晨光)/ `result_defeat`(碎盾暮色),1536×1024 横。验收同指令卡:单焦点、情绪一眼可读。

## 数量与工时预估

| 类别 | 唯一资产 | 预计生成量(含候选/重roll) |
|---|---|---|
| 卡面 | 49 | ~150-200 张 |
| 领袖+Key Art+棋盘+结算 | 7 | ~25 张 |
| UI 套件 | 12 | ~35 张 |
| **合计** | **68** | **~200-260 张** |

人肉筛选(四选一)按卡面 2~3 秒/张判断 + UI 细看,总投入约 **3~4 小时**,建议分 3 批:①棋盘+UI 套件(先让战场换装)→ ②49 卡面 → ③主菜单+结算。

## 建议生成顺序

1. **C 棋盘 + E 卡框/宝石/卡背** — 最先让 BattleScene 脱掉几何占位,整体观感立刻成型
2. **A 卡面 49 张** — 大头,分阵营批量过(同阵营连续生成一致性更好)
3. **B 领袖 → D Key Art** — Key Art 依赖领袖人设定稿,顺序不能反
4. **F 结算图** — 等 P4 做结算循环时再出

---

## G. v2 新阵营增补 —— 黄昏教团 & 掘世匠会(2026-07-17,依据 docs/06 卡表 + docs/07 附录 A/B)

前置(顺序硬性):① style_bible.json 补 duskweaver/undervault 两个阵营色块(docs/07 附录 B,**色块可先行,不等代码**);② 卡面类提示词要等 X1 的卡牌 JSON 入库后重跑 `generate_prompts.py` 自动产出——art_prompt 全文在 docs/07 附录 A,**不手写提示词**的规则不变。

### G.1 卡面插画 — 34 张

| 项 | 规格(全部沿用 §A) |
|---|---|
| 单位卡 23 张 | dw_* 单位 10 + 衍生物 1、uv_* 单位 11 + 衍生物 1;1024×1536 竖版,全身立牌标准 |
| 指令卡 11 张 | dw_* 6 + uv_* 5;1536×1024 横版,动作小景标准 |

**新增验收点**:
- 教团卡面必须有"烬橙光源"(火是阵营身份,没火=打回);但**背景不可整片火海**,否则去背立牌轮廓粘连。
- 匠会卡面必须出现铆钉/齿轮/蒸汽管至少其一(与中立青铜色的区分手段,见附录 B 自检);**架设类单位(路桩/弩炮塔/攻城平台/贯日/哨戒炮)要画出锚定结构**(螺栓/地钉/基座),立牌一眼读出"这东西不会动"。
- 衍生物 2 张(烬火雏凤/哨戒炮)按单位卡标准,但主体允许更小巧——它们在场上就是"小件"。

### G.2 领袖头像 — 2 张

`leader_dw_vela`、`leader_uv_brom`,规格与验收同 §B(1024×1024,圆裁内脸+纹章完整)。
**加验**:与 key_art_main 上的旧两位领袖并排,气场不虚;薇拉忌讳画成"邪教反派脸"(她是放逐学者,冷静>疯狂);布罗姆忌讳画成通用矮人铁匠(他是总工程师,拿图纸不拿锤子)。

### G.3 UI 增补 — 4 项

| id | 用途 | 尺寸 | 关键验收点 |
|---|---|---|---|
| `frame_duskweaver` | 教团卡框 | 1024×1536 竖 | **插画窗与既有 3 框逐像素同位**(窗区 16.5%~84% × 15.2%~68.8%);紫黑底+烬橙描线 |
| `frame_undervault` | 匠会卡框 | 1024×1536 竖 | 同上;铁灰底+黄铜铆钉边 |
| `emblem_duskweaver` | 教团纹章 | 1024×1024 | 剪影建议:环形余烬中的倒置火焰;单色化可辨 |
| `emblem_undervault` | 匠会纹章 | 1024×1024 | 剪影建议:齿轮环抱的炮管断面;单色化可辨 |

卡框生成时挂既有 frame_iron_vow/frame_wildpack 作参考图(同套模具感)。

### G.4 可选项(默认不做)

- `key_art_main` v2(四领袖同框):等 4 阵营都有玩家基础再换门面;当前主菜单不动。
- 架设单位专用立牌底座(带地钉的 standee_base 变体):X3 先用调制色代替,手感好再补。

### G.5 数量与批次

| 类别 | 唯一资产 | 预计生成量(含候选) |
|---|---|---|
| 卡面+衍生物 | 34 | ~100-140 张 |
| 领袖 | 2 | ~8-12 张 |
| UI(框/纹章) | 4 | ~12-16 张 |
| **合计** | **40** | **~120-170 张** |

**批次 1(不等代码,色块就绪即开工)**:G.3 卡框/纹章 → G.2 领袖。菜单与卡牌先成型。
**批次 2(等 X1 JSON 入库重跑提示词)**:G.1 卡面 34 张,分阵营连续生成(一致性),教团先(单位少)。
人工筛选预估 **~2 小时**。

---

## H. v3 第二批扩充卡牌(2026-07-18,依据 docs/10 §1–§5)

**状态:已生成并完成首轮验收。** 本批共 80 张新卡:中立 24 张、铁誓/荒野/黄昏/匠会各 14 张。
提示词由已入库卡牌 JSON 的 `art_prompt` 经 `generate_prompts.py` 与锁定的 v1 风格圣经拼装,
本批未修改 `style_bible.json`。

### H.1 产物与规格

| 类别 | 数量 | 原始生成图 | 游戏资产 |
|---|---:|---|---|
| 单位卡 | 65 | `generated/v1/<id>.png`,1024×1536 | `game/assets/art/cards/<id>.png` 512×384 + `standees/<id>.png` 512×672 RGBA |
| 指令卡 | 15 | `generated/v1/<id>.png`,1536×1024 | `game/assets/art/cards/<id>.png` 512×384 |
| 合计 | **80** | **80 张定稿 + 3 张 rejected-v1 留档** | **80 卡面 + 65 立牌** |

阵营分布:

- 中立:`nl_*` 24 张(21 单位 / 3 指令)
- 铁誓军团:`iv_*` 14 张(11 单位 / 3 指令)
- 荒野游群:`wp_*` 14 张(11 单位 / 3 指令)
- 黄昏教团:`dw_*` 14 张(10 单位 / 4 指令)
- 掘世匠会:`uv_*` 14 张(12 单位 / 2 指令)

### H.2 设计与验收结论

- 全批继续使用三张 v1 锚点:铁誓单位、荒野单位、铁誓指令场景;并排观感保持同一游戏。
- 单位卡均按单一主主体、完整头脚、可分离轮廓出稿;指令卡均为横版单焦点动作小景。
- 教团 14 张均保留紫黑材质与烬橙光源,未使用整片火海充当背景。
- 匠会 14 张均有铆钉/齿轮/蒸汽管等工程语言;架设单位可见支脚、地钉或固定基座。
- 中立使用青铜/赭黄与实用军备,未引入四阵营专属纹章。
- 机器验收:80/80 卡面尺寸正确;65/65 立牌尺寸正确、四角透明;无缺失 ID。
- 定向重做:`iv_shield_brother`、`nl_longbow_line` 从多主体改为单主体;
  `dw_inferno_behemoth` 收束横向火焰以保证去背轮廓。旧稿以 `-rejected-v1.png` 留档。

### H.3 QA 总览

接触表位于 `tools/art/out/exp2_contact_*.jpg`;立牌全量总览为
`tools/art/out/exp2_standee_all.jpg`,关键复杂单位抽检为 `exp2_standee_qa.jpg`。

---

## I. Rules 0.9.0 教团法术改版与秘密体系(2026-07-21,依据 docs/21 §8)

**状态:已生成、后处理并导入 Godot。** 新卡数据尚未入库时先在风格圣经 `extra_assets`
中固定 ID;后续卡牌 JSON 入库须沿用下表 ID,避免资源重命名。

### I.1 新绘卡面

| id | 名称 | 类型 | 游戏规格 |
|---|---|---|---|
| `dw_molten_sword_priest` | 熔剑祭士 | 单位 | 512×768 卡面 + 512×672 RGBA 立牌 |
| `nl_stealth` | 匿踪 | 指令 | 768×512 |
| `nl_spell_ward` | 法术护体 | 指令 | 768×512 |
| `dw_smoke_bomb` | 烟幕弹 | 指令 | 768×512 |
| `dw_ash_bind` | 灰缚 | 指令 | 768×512 |
| `dw_oath_counter` | 焰誓反制 | 指令/秘密 | 768×512 |
| `dw_ember_trap` | 烬火陷阱 | 指令/秘密 | 768×512 |
| `dw_wildfire` | 燎原 | 指令 | 768×512 |

`薪火回响`按 docs/21 决策复用 `dw_first_ritualist.png`,仅改卡名,不重复生成。

### I.2 UI、状态与格子特效

| id | 用途 | 输出 |
|---|---|---|
| `icon_trap_hidden` | 己方未现形陷阱格标识 | `ui/`,最长边 256 RGBA |
| `fx_trap_revealed_fire` | 现形后持续火焰 | `fx/`,512×512 RGBA |
| `fx_smoke_zone` | 5 格十字烟幕覆盖 | `fx/`,512×512 RGBA |
| `status_rooted` | 定身 | `ui/`,最长边 256 RGBA |
| `status_hidden` | 潜行 | 同上 |
| `status_spell_ward` | 法术护体 | 同上 |
| `status_charge` | 蓄能 | 同上 |
| `status_molten_sword` | 熔岩巨剑 | 同上 |
| `status_growth` | 成长倒数底框 | 同上;数字由程序叠加 |
| `counter_kindle_power` | 加深·蓄能双计数器 | 同上;两格数字由程序叠加 |
| `marker_secret` | 对手暗牌存在标识 | 同上;`?×N`由程序叠加 |
| `icon_kindle_damage` | 薪炎伤害飘字小图标 | 同上 |

### I.3 验收记录

- 原始生成图:8 张卡面为 1024×1536 / 1536×1024;12 张 UI/特效为方形高清源图。
- 游戏资产:8/8 卡面、1/1 立牌、10/10 UI 图标、2/2 RGBA 特效均存在且尺寸正确。
- 烟幕严格为 5 团十字形;烟幕/陷阱火焰由纯黑源图转换为透明 alpha,四角透明。
- 图标内无文字、问号或数字;计数与秘密数量全部预留给程序叠加。
- QA:`tools/art/out/docs21_cards_contact.jpg`、`docs21_ui_raw_contact.jpg`、
  `docs21_ui_final_contact.jpg`。

---

## J. 掘世匠会单核炮台与模块升级(2026-07-21,依据 docs/20 §7)

**状态:需新绘的 5 张已生成、后处理并导入 Godot。** §7.1 指定复用的 19 项旧图均未
重新生成或覆盖;被砍卡原图继续保留。模块卡使用 `prop` 方形构图,不上场且不生成 standee。

### J.1 新绘清单

| id | 名称 | 构图 | 游戏资产 |
|---|---|---|---|
| `uv_mod_siphon_shell` | 汲能弹头 | prop | `cards/uv_mod_siphon_shell.png`,512×512 |
| `uv_mod_siphon_core` | 汲能核心 | prop | `cards/uv_mod_siphon_core.png`,512×512 |
| `uv_mod_concussion` | 震撼弹 | prop | `cards/uv_mod_concussion.png`,512×512 |
| `uv_mod_tracked_chassis` | 履带底盘 | prop | `cards/uv_mod_tracked_chassis.png`,512×512 |
| `uv_mirror_works` | 镜像工坊 | order | `cards/uv_mirror_works.png`,768×512 |

### J.2 验收记录

- 4 张模块均为单一完整零件、微俯视、四边留白;无人物、完整炮台或多余散件。
- 汲能Ⅰ/Ⅱ通过管线复杂度与红金能量强度区分;震撼弹有钝击弹头和冲击环;履带底盘不是完整载具。
- 镜像工坊的双机械臂、相同炮件与镜面高光在手牌缩略图尺寸仍可读。
- 5/5 卡面尺寸正确,无 standee;Godot `.import` 与缓存完整。
- QA 总览:`tools/art/out/docs20_new_art_contact.jpg`。

### J.3 工造炮台动态外观补强(2026-07-22)

- 卡面插画 4 态:裸炮、快速装填机、主炮核心、主炮核心+快速装填机。
- 棋子采用“4 种上装 × 3 种底盘”组合,共 12 态;底盘为三脚架、架设平台、履带底盘。
- 同时装备架设平台与履带底盘时,规则加成均保留,棋子显示最后装配的底盘,确保每次装配都会改变轮廓。
- 生成脚本:`tools/art/build_turret_variants.py`;QA:`tools/art/out/turret_v2_card_contact.png`、`tools/art/out/turret_v2_standee_contact.png`。

---

## K. 平衡补丁 #6 铁誓两张新指令(2026-07-25,Rules 0.14.0)

**状态:已生成并完成后处理(2026-07-25)。** 提示词由 `generate_prompts.py` 随新卡 JSON 入库后重跑产出,取 `out/prompts.json` 的
`iv_guardian_vow` / `iv_counter_horn` 两条,**不手写**。三张改数值的老卡(墙垛弩卫 / 门岗新丁 / 持盾卫)
**沿用原图,不重绘**——改的是数值与关键词,不是形象。

### K.1 新绘清单

| id | 名称 | 类型 | 构图 | 生成尺寸 | 游戏资产 |
|---|---|---|---|---|---|
| `iv_guardian_vow` | 以身为盾 | 指令 | order(横版动作小景) | **1536×1024 横** | `cards/iv_guardian_vow.png` 768×512 |
| `iv_counter_horn` | 反击号角 | 指令 | order(横版动作小景) | **1536×1024 横** | `cards/iv_counter_horn.png` 768×512 |

指令卡**不出立牌**,不进 `standees/`。每张出 **2~4 候选**四选一,原图存 `generated/v1/<id>.png`
(文件名与 id 严格一致),后处理 `python postprocess.py --ids iv_guardian_vow iv_counter_horn`
(或 `--cards-only`,不触发 rembg)。

最终选图均使用 v1 三张锚点保持风格一致:`iv_guardian_vow` 采用肩甲代挡长矛的候选,
`iv_counter_horn` 采用城垛号手与守军越墙反攻的候选。原图均为 1536×1024,
游戏卡图均为 768×512;两张指令卡均未生成 `standees/` 资产。

### K.2 验收标准

沿用 §A 指令卡通用标准:

- **单一视觉焦点**的动作小景;缩到手牌大小(约 120px 宽)**仍能一眼读懂在干什么**;边缘暗化虚化,不画完整风景。
- 冷灰暖褐主调 + 辉尘青金点缀;并排放进已验收图堆里不跳戏。
- 画内**无任何文字/数字/卡框/边框**;无 3D 渲染感、无赛璐璐动画感。
- **阵营辨识(铁誓)**:钢灰蓝 + 誓火金;盾墙 / 城垛 / 誓言纹章任取其一,盖住卡名能认出是铁誓即合格。

逐卡加验(读图即读牌效,这两张是新机制的唯一图示):

- `iv_guardian_vow` **以身为盾**(授予守护 = 相邻友军的伤害转移到它身上):画面必须读出**「替人挨打」**——
  一名铁誓战士**横身挡在受伤同伴之前**,用肩甲/盾面**接下本该打在同伴身上的那一击**。
  两人关系要清楚(挡者在前、被护者在后且明显更狼狈),**不要画成两人并肩举盾**(那是盾墙号令的画面)。
- `iv_counter_horn` **反击号角**(驻防加成变永久 = 守军带着加成压上去):画面必须读出**「从守转攻」**——
  城垛上**吹号**的号手 + 守军**越出墙线向前涌出**的动势,方向感朝画面外/前方。
  **不要画成固守/闭门**,也不要让号手占满画面而看不到出击的队列。

### K.3 可选(默认不做)

- 两张新卡的 `card_art_framing.json` 取景在开发模式「插画取景」里逐卡保存,**出图后再调**;缺图时客户端自动回退几何占位,不阻塞发版。
